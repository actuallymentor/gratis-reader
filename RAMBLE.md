I'm going to ramble an app idea at you that we're gonna structure later. So what I want is a progressive web app that is an e-reader, and when I say that I mean you can upload common ebook formats like EPUB or Mobi. And this reader is designed to help me learn a new language. It does 
that by allowing me to whenever I open a book, select a language that is not the origin language. So for example, the origin language of the book might be English. And I might select a different language like Albanian. I then also select a language level, which is split between 
toddler, primary schooler, high schooler, and adult. You can also use commonly used language level classifiers like A1, B1, and so forth, but I always want them to be paired with a description that makes it easy for someone who doesn't know what the language levels are. The eBUB 
reader, then instead of showing me the original language, will translate the page that I'm looking at to another language. So the one I selected, and will take great liberty in translating it when changing a language level. So the goal of this translation is definitely not accuracy. 
But for example, if I input a book that is written for an adult, the rewritten translation on toddler level will really simplify things. A sentence like the gothic style house had an architecture that really inspired his soul might be translated as the house was beautiful. It's 
really really just about an approximate translation that respects the language level that the user selected.

The translation of this ebook will be done through open router. So when you first land on the app screen, it will just ask for an open router API key. Open router will be used for translation, and it will use some relevant model, some LLM model in the backend. You may use the open AI models for this by default. But we will be using open router as the router in front of the LLMs. You'll of course have to define prompts like system prompts and prompts that go around the sentences that we're gonna translate to make the AI translate sentences for you. You're gonna have to think deeply about how to make the AI comply properly.

From a user experience, what I want is that the current page is translated. So that's the page that's currently in view, as well as two pages ahead. This way we don't waste a lot of API usage on translating an entire book. So whenever we skip to a page, which might be skipping to the next page or jumping within the book. We'll always make sure that the two next pages are translated. So we're translating ahead. We'll be keeping a cache of these translations in local storage. So we don't have duplicate LLM queries, because LLM queries are expensive. Also optionally, in case the internet is down, we will fall back to this cache so that we read as much as possible. You're gonna have to think about how to tag these sentences, so you're probably gonna have to have some sort of identifier per book. Identifier per sentence, identifier per language level. And what I want you to keep in mind is the unit of translation that we're gonna be doing is sentences. So we're gonna translate sentences sentence by sentence. Though for the purpose of translation, we might as context give sentences before and after, or even an entire paragraph. The reason we're going to translate sentence by sentence is on a user experience level, whenever the user hovers a word, we want to show the original word. Or at least this word translated back to the original language. Because of course the original word might not be available given that we're translating not exactly literally if we're jumping language levels. When a sentence is tapped, the sentence will swap back from the translated language to the original. Then when tapping again, it goes back to the translated language. When a sentence is long pressed, so pressed and held, a little popover will appear, like a little modal box that will show the original sentence, the translated sentence, and explains why this sentence is translated the way it is. Perhaps word by word and some nuances in in case that is relevant. Of course, this detailed explanation is not something that we're going to be creating beforehand that'll be created on the fly. Otherwise, we'll be wasting a lot of LLM tokens on this. For the hovering of words use a tooltip.

Note that once initially the API key for open router has been input, we're gonna cache it locally, of course. And then the application will have kind of a library view where any books that the user has inputted into the app are available and are stored within the app's storage. So there's multiple books that the user could open. And then within the book, there will of course be a little configuration icon at the top right where the user could swap between language levels. Also includes some common e-reader configurations like text size, and perhaps even font styles.

During the development process, I really want you to test what you're building. So I want you to spin up an actual browser and open the app and make sure that the app is actual doing LLM inference. I want you to try and hover things, tap sentences and so forth. Make no assumptions about whether the code you're running is correct. I want you to have automated tests, which are end to end, ideally with playwright. But aside from that, I also want you to liberally use the MCP server with a browser of your choice. So that could be Chrome, Chromium, Firefox, whatever works for you. And for you to click around in the application and check whether everything works the way that it should. Always check your assumptions. Don't assume that the code you're written is correct.

while developing you may use the open router API key that is stored in .env.
